= render 'profile/header'
.container#profile_content
  .ui.four.items
    - for item in @items
      - if item[:score][:likers]
        - if item[:score][:likers].include? @user.id
          .mo.item#squire
            a.thumb href="#{item[:url][:show]}" data-no-turbolink='true'
              - if "#{item[:thumb]}".empty?
                img src="/noimg.jpeg" 
              - else
                img src="#{item[:thumb]}" 
            .details
              .inside
                .title 
                  a href="#{item[:url][:show]}" data-no-turbolink='true'
                      = item[:title]
                .rating  = item[:score][:like]